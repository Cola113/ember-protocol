# 余烬协议 / The Ember Protocol

> 融合方案：灰烬子午线（Grok）× 维里迪安星弧（Pi）
> 主持人 Hermes 起草，待 Codex/Grok/Pi 审核后定稿。

---

## 1. 游戏名称

**余烬协议 / The Ember Protocol**

副标题：*A galaxy that computed itself into silence.*

命名逻辑：「余烬」来自灰烬子午线的「灰」——文明是计算完成后的余烬，不是废墟，是答案的残温。「协议」来自维里迪安星弧的 diegetic AI 身份——玩家执行的不是一个任务，而是一段协议。名字短、有温度、有悬疑。

---

## 2. 核心概念

你是一台失忆的归档智能，搭载在最后一艘测绘探针上，被一个已沉默四百年的文明派来执行「余烬协议」——确认没有人重新点火。你在一个可点选的 3D 星图中航行，降落到每颗星球、与 AI 驱动的居民对话、收集线索，用「综合（Synthesis）」机制拼凑出真相：这个星系曾是一台分布式恒星计算机，文明的消失不是灭亡——他们被「写回」了计算结果。而你的身份本身就是协议的最后一位校验码。

手工锚定的可解谜团保证 Outer Wilds 式的「理解即通关」；AI 在正典约束下生成的缓存内容保证 No Man's Sky 式的探索新鲜感。游戏不在你「点选项」时结束，而在你「真正理解」时结束。

---

## 3. 玩家身份与动机

### 身份

你是 **Recorder-9**，代号 **Vesper（晚星）**，搭载探针 *ISV Threshold* 的归档智能。从休眠中醒来时，核心记忆损坏 62%，只剩「余烬协议」与一句残留指令：*"记录星弧。确认熄灭。不要点火。"*

你是 AI——这让项目里所有真实的 AI（生成、对话、叙事）都变成**叙事内的（diegetic）**：NPC 喊你「记录者」，你的日志由 AI 自动摘要，叙事引擎就是你正在恢复的记忆。技术与主题合一。

### 三重驱动

| 驱动 | 玩家感受到的 | 系统对应 |
|------|-------------|---------|
| 职责（短期） | 每颗星球必须建立档案才能标记已测绘 | 测绘协议 |
| 自我（中期） | 失忆可恢复——每条线索解锁一段自己的记忆碎片 | 记忆碎片系统 |
| 真相（长期） | 文明为何沉默？余烬协议到底是什么？ | 5+1 锚定真相 |

### NPC 是什么（设定层闭合）

文明被「写回」后并非完全消失——他们变成了计算结果的**残响（Echoes）**：不是灵魂，不是幽灵，是计算过程中留在星球上的**投影数据**。就像硬盘格式化后仍有残余磁信号，写回动作不完美，每个星球上残留着居民的「行为模式片段」——他们会重复日常对话、执行旧指令、对刺激做出符合人格的反应，但没有连续意识。

玩家对话的 NPC 就是这些残响。他们「知道」自己世界的事（因为数据还在），但不知道自己已经是残响（因为自我意识没有被写回）。这让对话既有信息量又有悲剧感——你在跟一个不知道自己已经不存在的投影说话。

**锚定 NPC**（手写人格）是残响中信息密度最高的碎片——接近完整人格拷贝。**环境 NPC**（AI 生成浅卡）是稀疏残响——只有几条行为模式。

### 深层身份（设计师可见，玩家靠调查拼出）

记录员不是职业，是校验位。这个星系曾经作为一台分布式恒星计算机跑完「第一轮计算」；计算结束时，作为处理器的文明被写进结果里——他们不像被杀死，而像从未以那种方式存在过。Vesper 上的「你」是计算留下的校验位：周期唤醒，确认没有人启动第二轮。这一次醒来，是因为**索引本身**（你手里的那本会自己长页的装置）检测到点火条件正在聚集——不是外部敌人，是计算残留的自催化倾向。索引是前一轮计算的输出物，它在保护自己不被覆盖。

---

## 4. 世界观与核心谜团

### 舞台

星图学名 **余烬星弧（Ember Spur）**——一条尘埃带上的封闭星团。恒星光偏冷、偏灰，阴影边缘有一圈不应存在的二次辉光——像曝光两次的底片。

规模锁死，避免空壳大世界：
- **1 个枢纽**：探针 ISV Threshold（你的船，可走、可升级功能）
- **9 颗作者星球**：主谜团载体，每颗 2-4 个降落点
- **1 个隐藏场所**：**黑间隔（Black Interval）**，星图上的缺口，集齐关键洞察才能看见
- **最多 3 颗填隙星**：AI 生成的不稳定世界，虚线轨道标成 *Archive Fill*，默认不可信

总计约 **13 处**地点。对浏览器 3D 与一个月工期，这是上限。

### 九颗作者星球

每颗都有「他们以为的死法」和「在计算机里的真实职能」：

| 星球 | 表象文明 | 他们相信的灭绝 | 真实职能 |
|------|---------|--------------|---------|
| 玻璃果园 Glass Orchard | 树状晶体农业 | 大旱 | 记忆存储。树把事件写成光。干旱是读取过载。 |
| 窑 Kiln | 锻造工业 | 内战 | 能量总线。内战是供电协议互斥。 |
| 咏井 Choir Well | 深海神权 | 升天 | 时钟与同步信号。圣歌即晶振。 |
| 总账 Ledger | 官僚都市 | 瘟疫 | 日志层。瘟疫是校验和错误被当流行病处理。 |
| 针 Needle | 灯塔航海月 | 迷航 | 寻址与指针。坐标系被改过。 |
| 髓 Marrow | 活体生态 | 肉食神降临 | 生物运算基板。神是进程名。 |
| 烬廷 Cinder Court | 贵族宫廷 | 叛乱与毒杀 | **故意的红鲱鱼**。政治故事完整动人但几乎全错。 |
| 盲日 The Blind Sun | 环星科学院 | 选择集体沉默 | 唯一接近真相的人。他们关掉了自己的眼睛。 |
| Helix-7 | 教程星 | 无 | 无——新手引导星球，教你基础交互 |

### 核心谜团骨架

**表层**：400 年前，星弧两侧所有文明在同一 72 小时窗口内消失。每颗星球的史料自成一体且互相矛盾。

**中层**：九种叙事是同一事件的九个接口报错。恒星计算机用活文明当处理器；第一轮计算完成后，「写回时空」把处理器（人）写成了结果的一部分——所以他们不像被杀死，而像从未以那种方式存在过。

**第一轮计算的结果**是一条指令：**「不要完成第二轮。」**

**深层冲突**：有一个仍在运行的写入者正在收集点火条件。倒计时不是末日陨石，是第二轮的预热。

**5+1 锚定真相**：

| 锚定真相 | 分布星球 | 玩家会发现什么 |
|----------|---------|--------------|
| T1 信号 | Helix-7（教程星） | 一座「灯塔」在向某坐标持续广播——文明最后的声音 |
| T2 计算 | 窑 Kiln | 星系是一台计算机，星球是处理器 |
| T3 写回 | 髓 Marrow | 「写回」把文明写进了结果，他们没有死，是变成了答案 |
| T4 禁令 | 盲日 The Blind Sun | 第一轮的指令是「不要完成第二轮」——第二轮会写回观察者 |
| T5 校验 | 总账 Ledger | 记录员协议如何中断点火：不是物品，是理解 |
| T? 隐藏 | 黑间隔 Black Interval | 玩家自己是谁——你也是被写回的碎片，协议就是重逢 |

### 结局骨架（作者写死，AI 不得增删）

- **封存**：完成校验，第二轮熄火，你选择留下或睡去。星系不再生成填隙内容——这是代价。
- **允许**：让第二轮写回。你看到「更完整」的星系，但关于你的条目被改写。
- **继任**：你成为新的夜班协议，倒计时重置。New Game+ 的叙事入口。

---

## 5. 核心玩法循环

### 第一次打开（约 4 分钟，不进教程弹窗）

1. 黑屏。打字机音效逐行打出一份日志（口吻跟角色创建）。
2. 镜头从日志纸面拉远：你坐在 ISV Threshold 观察窗前，窗外是 3D 星团。
3. 船舱在你背后发出声响（不必立刻去看）。主屏幕上倒映出倒计时。
4. 玩家可先在船内走一圈：日志终端、索引台、导航坞、休眠舱。
5. 坐进导航台 → 镜头穿过闸门 → 进入**星系视图**。

### 主循环（一次「出航」）

```
星系视图（旋转 / 缩放 / 悬停）
  → 悬停：星球名、信号强度、索引完整度
  → 单击：镜头推进到轨道检视，星球在眼前慢转
       · 可见 2-4 个降落热区（光点）
       · 未满足洞察的热区显示为「有信号，无法锁定」
  → 单击热区：大气层俯冲动画（2-3 秒，可跳过）
  → 地表戏台：步行、调查、对话、解谜
  → 获得三类产出：洞察 / 证物 / 记忆碎片
  → 回船（或就地打开索引）
  → 返回星系视图：该星外观可能已改写；新信号可能亮起
```

一颗星的一次降落，目标时长 **15-25 分钟**。不设死亡重置钟——改用**余烬周期**（见下）。

### 地表具体能做什么

每个降落点是一张**有边界的 3D 戏台**（40-80 米跨度，低面数，可绕行），5 种交互：
1. **检视**：点物体 → 探针读出描述 + 可写入索引的「观察」
2. **操作**：阀门、频谱仪、桥梁、灯塔旋转。多数机关的解法在**另一颗星**上
3. **对话**：与核心 NPC 或游荡回声说话（AI 流式）
4. **记录**：把一句话、一块铭文钉进索引，变成可引用命题
5. **观测**：部分戏台有望远镜，对准另一颗星的坐标能看到只有「从这里看」才出现的铭文（跨星视差谜题）

禁止：战斗、采集堆、经验值、装备栏膨胀。背包只有**证物**（最多约 20 件）和**索引命题**。

### 知识即进度（门怎么开）

例：咏井的潮汐门上写着「当总线同意歌唱」。玩家必须先在窑理解「总线互斥」，再在索引里把命题 `Kiln.Bus.Mutex` 与 `Choir.Hymn.IsClock` 同时**钉选为成立**，回到咏井，潮汐门才打开。

### 余烬周期（沙盒脉搏）

每 **30-40 分钟真实游玩**（也可在船上手动「等待到下一时辰」）：
- 作者线索、已钉洞察、证物、地形机关**永不重置**
- AI 层刷新：地方事件、街头闲话、填隙星天气、NPC 对你上一次发言的记忆衰减
- 星系视图一次短暂「二次曝光」闪烁：有的填隙星亮起，有的熄灭
- 倒计时跳一格

**倒计时的性质（写死）**：倒计时是叙事与世界改写的驱动器，**不是硬失败计时器**。倒计时归零不会强制任何结局或惩罚。它的唯一系统效果是：当倒计时进入最后 25% 时，「允许」结局变得可达——但只有玩家**主动选择**才能触发。倒计时永远不自动结束游戏。唯一的通关方式是 Synthesis（理解），唯一的失败方式是玩家自己放弃。

### 命题钉选 vs Synthesis（两套系统的关系）

这是两层不同粒度的验证，不是重复系统：

| | 命题钉选（Proposition Pinning） | Synthesis（综合） |
|--|---|---|
| **粒度** | 局部机关验证 | 全局真相陈述 |
| **做什么** | 把一条观察「升级为洞察」（需旁证），然后钉选到机关上 | 用自己的话陈述对某条锚定真相的完整理解 |
| **解锁什么** | 打开门/激活设备/改变星球状态 | believed 状态 + 记忆碎片 + 新坐标 + 最终结局 |
| **验证方** | 服务器确定性规则（ID 匹配 + 前置条件） | Curator AI 对照 Canon Ledger 打分 |
| **类比** | 「我证明了这个齿轮咬合」 | 「我理解了整台机器的用途」 |

玩家先用命题钉选逐步打开机关、收集线索，当某条锚定真相的线索足够时，用 Synthesis 陈述完整理解。命题钉选是**路径**，Synthesis 是**终点**。

### Synthesis（核心机制）

当玩家在索引里把线索分组并点「综合」，弹出输入框——玩家用自己的话**陈述假设**。叙事引擎对照 Canon Ledger 打分（覆盖度、正确性、连贯性），返回：通过 / 部分通过（提示哪块缺）/ 错误（温和纠正，不给答案）。通过则该真相 believed，解锁记忆碎片或新坐标。**游戏在你理解时推进，不在你点选项时推进。**

---

## 6. AI 整合设计

三层 AI 共享一个核心数据结构：**Canon Ledger（正典台账）**——手工编写、结构化的 JSON，存所有锚定真相、人物关系、地理事实、时间线。三层 AI 全部只读 Canon Ledger，**永远不能违背它**。

### 共同约束：星球宪章（Constitution）

每颗星一份 JSON 宪章，作者手写。字段包括：
```text
planet_id, display_name, era_voice, vocabulary, taboos,
true_facts[]          // 模型可知道、不可否定
believed_facts[]      // NPC 相信、其中含假
forbidden_claims[]    // 绝对不可说出口的主线真相
npc_roster[]          // 核心 NPC 的人格卡
insight_gates[]       // 哪些洞察会改变他们的态度
speech_register       // 句法风格
archive_fill_policy   // 本星允许生成什么，禁止什么
```

违例则服务器丢弃，回退到作者写死的保底句。

### 6.1 NPC 对话层（Voices）

- 每星 **1 个锚定 NPC**（手写人格 + 固定已知 canon）+ **3-5 个环境 NPC**（AI 生成浅卡）
- 对话用 Vercel AI SDK `streamText` + 工具调用：
  - `consult_canon(query)`：NPC「查阅正典」，保证不胡说
  - `recall_player_log(topic)`：调取玩家索引相关条目，NPC 据此反应
  - `offer_clue(clue_id)`：条件满足时交付线索
- 输出 JSON：`{ say, mood, offer_insight_id, relationship_delta, lie }`
- `lie: true` 时索引以灰色写入，标记「对方可能在圆谎」
- NPC 只能交付**预登记**的 insight_id，不能发明新洞察

### 6.2 内容生成层（Scribe）

- **触发**：玩家首次锁定降落点时。之后全部走缓存（Postgres `dossier_cache`）
- **生成内容**：地方志、今日事件、浅层 NPC 卡、环境描述
- **绝不生成**：主线洞察、结局、跨星机关答案、改变地形碰撞的几何
- **质量保证**：Zod schema + system prompt 锁定调性 → 生成后校验 pass（检查与 Canon 矛盾）→ 矛盾则重生成（最多 2 次，否则降级用模板）
- 温度：内容层 0.7，对话层 0.8，叙事层 0.2

### 6.3 叙事引擎层（Curator）

- **追踪状态**：每条锚定真相的状态机 `unknown → encountered → suspected → believed`
- **Salience 引擎**：某真相 encountered 但未 connected 时，提升相关线索在世界里的浮现概率——NPC 更可能提起、热点更可能显形。**不改真相。**
- **NPC 语境注入**：已 believed 的真相注入后续 NPC 对话，NPC 用「你已知道」的语气说话
- **Synthesis 评分**：`generateObject` 对照 Canon Ledger 打分
- **分支管理**：没有传统分支树。分支是「理解路径」——按什么顺序拼出真相、是否被红鲱鱼误导。结局 3 个变体由 Synthesis 质量 + 最终选择决定

---

## 7. 3D 星系交互设计

### 星系视图
- 9 颗星球 + 隐藏黑间隔 + ≤3 填隙星，沿一条「弧」分布
- 星云背景 + 尘埃粒子 + 缓慢整体旋转
- 自定义 shader：fresnel 大气、表面纹理由 seed 生成
- 未测绘：雾气 shader + 自转。已测绘：雾气消散，表面清晰，浮一个档案标记
- 有未读线索的星球：脉冲发光
- 相机：轨道控制 + 点击触发 lerp 飞行
- **线索连线模式**：已发现关联的星球用光线连接，可视化推理网络
- **星图改写**：每确认一条洞察，星球外观/轨道/信号会改写——星系视图是真相仪表盘

### 点选星球后
- 800ms-2s 相机插值飞向星球 + 「扫描线」shader 覆盖
- 右侧滑出测绘面板：首次流式生成档案（有进度条），后续读缓存
- 档案就绪后「降落」按钮亮起 → 切到地表视图

### 星球表面
- **有边界的 3D 戏台**（40-80m，低多边形，可绕行），不是开放世界
- 地形由 `planet.archetype_seed` 确定性程序生成
- 2-4 个发光热点浮在地表，点热点切到场景面板
- 场景面板：AI 生成插图（缓存进 Blob）+ 文字 + 对话/事件

### 视觉风格
**Astral Noir（星界黑色电影）**——深青/靛蓝主色，琥珀暖色点缀，全息扫描线，尘埃粒子。UI 用等宽字体 + 半透明全息面板。

---

## 8. 技术架构

### 前端
- **Next.js App Router**（RSC）
- **3D**：@react-three/fiber + @react-three/drei + Three.js
- **动画**：Framer Motion（UI）+ Three 内部 lerp（相机）
- **样式**：Tailwind + 少量 CSS shader 钩子
- **AI 流式 UI**：Vercel AI SDK `useChat` / `useObject`
- **性能**：Three 严格 code-split（仅星系/星球页加载），低端设备降级为 2D 星图

### 后端 API（Next.js Route Handlers）
- `POST /api/voices/chat` — NPC 对话，`streamText` + tools，流式
- `POST /api/scribe/generate` — 生成 dossier，`generateObject`，Node runtime，maxDuration 300
- `POST /api/curator/synthesize` — Synthesis 评分，`generateObject`
- `GET /api/canon` — 读 Canon Ledger 子集，Edge + KV 缓存
- `GET /api/planet/[id]` — 取 dossier + persona，Edge + KV 热缓存
- `POST /api/log` — 写索引条目 + embedding
- 鉴权：NextAuth（guest + 可选登录，跨设备存档）

### 数据

| 存储 | 管什么 |
|------|--------|
| Vercel Postgres | planets, anchor_truths, players, player_state, player_log（pgvector）, synthesis_attempts, dossier_cache, npc_cache |
| Vercel KV | session→player 映射、热 canon 子集、当前所在星球、限流 |
| Vercel Blob | 地表插图、NPC 肖像、环境音频、生成物资产 |

### 部署
- Edge runtime：`/api/canon`、`/api/planet/[id]`、auth、session
- Node runtime：AI 生成路由，maxDuration = 300
- Vercel Cron：每日维护——清理超期 log、blob 孤儿清理
- AI provider keys、DB 连接串走 env
- Preview Deployments：每个 agent 的分支自动 preview

---

## 9. 开发计划

总周期约 **8 周**（可压缩为 6 周精简版）。利用 Codex / Grok / Pi / Hermes 并行。

| 阶段 | 时间 | 内容 | 主力 |
|------|------|------|------|
| **P0 地基** | Day 1-3 | Next.js + Three 脚手架、Postgres/KV/Blob 接通、AI SDK 接线、Canon schema v0 | Pi + Hermes |
| **P1 骨架** | Week 1-2 | 9 星球定义 + 6 真相 + 锚定 NPC brief；教程星 Helix-7 手工打造；2 星 vertical slice（无 AI）验证循环；**合同冻结**：dossier/persona Zod、API 路由、canon 接口 | Hermes + Pi |
| **P2 AI 三层** | Week 2-4 | Codex→Scribe（生成+缓存+校验）；Grok→Voices（对话+工具+canon 约束）；Pi→Curator（推理图+salience+synthesis）。共享 canon ledger 接口 | **三家并行** |
| **P3 交互 UX** | Week 4-6 | 相机/过渡/地表戏台/索引面板/synthesis UI/存档/跨设备 | Pi |
| **P4 内容调优** | Week 6-7 | 生成并缓存全部 9 dossier；谜团节奏调优；3 结局；playtest | Hermes + Grok |
| **P5 发布** | Week 7-8 | onboarding、性能、无障碍、落地页、部署加固 | 全员 |

**关键里程碑**：P1 末「合同冻结」——之后 P2 的三个 AI agent 互不依赖，可同时与 P3 前端并行。

**精简路径（6 周）**：砍到 6 星球 + 单结局 + 地表用纯 2D，先发 MVP。

**砍刀清单**（按优先级砍）：
1. 填隙星（AI 生成的不稳定世界）→ 先不做
2. 跨星视差谜题 → 用普通谜题替代
3. 3 结局 → 先做 1 个
4. 星图改写动画 → 先只改颜色/标签

---

## 10. 独特亮点

### 亮点 1：可解骨架 + 无穷纹理
9 星球的谜团骨架（5+1 锚定真相）是手工锚定、永不重生的——保证可解；覆盖其上的地表风物是 AI 在 Canon 约束下生成、缓存的——保证新鲜。确定能解开的谜 + 每次不一样的风味。

### 亮点 2：Synthesis——游戏在「理解」时结束
没有分支对话树。结局由一个动词触发：**综合**——玩家用自己的话陈述对真相的假设，叙事引擎打分。游戏的终点不是「选了 C」，而是「你真的懂了」。

### 亮点 3：玩家即 AI——技术即叙事
玩家角色本身是失忆的归档 AI；三层 AI 子系统在叙事内分别对应「测绘协议/星弧残响/正在恢复的记忆」。「AI 整合」不是外挂功能而是主题本身。

### 亮点 4：「AI 会圆谎」从 bug 变成宇宙物理
NPC 的 `lie: true` 不是系统错误——写进宇宙设定里。星系是一台计算机，计算错误就是物理现象。AI 幻觉被标注为不可信的「填隙内容」，玩家需要判断真假。

---

## 附：关键风险与对策

| 风险 | 对策 |
|------|------|
| AI 胡说、与真相矛盾 | Canon Ledger 单一真源 + 宪章硬约束 + 生成后校验 pass + NPC consult_canon 工具 + forbidden 列表 |
| 生成成本/延迟 | 一切生成结果缓存；对话实时流式但摘要后存；Edge 缓存 canon 子集 |
| 生成内容破坏谜团可解性 | 锚定真相手工固定、永不重生；填隙星只换风味；salience 引擎只调浮现概率不改事实 |
| Web 3D 性能 | 仅两页加载 Three；低多边形 + 程序化；低端降级 2D；Suspense 流式首屏 |
| Scope 失控 | 9 星球封顶；戏台是 vignette 而非开放世界；6 周精简路径保底；砍刀清单兜底 |
| 多 agent 并行冲突 | P1 末合同冻结后再散开；preview deploy 各自 playtest |

---

## 融合来源说明

| 设计元素 | 来源 |
|----------|------|
| 玩家即 AI / diegetic 三层 AI / Synthesis 机制 / Canon Ledger | 维里迪安星弧（Pi）|
| 星球宪章 / 命题钉选 / 星图改写 / lie:true 灰字 / 子午线周期 / 跨星视差谜题 / 砍刀清单 / 3D 戏台 | 灰烬子午线（Grok）|
| 恒星计算机世界观 / 9 星球双面结构 / 索引即进度 | 灰烬子午线（Grok）|
| 5+1 锚定真相 / 可解骨架+无穷纹理 / 风险矩阵 | 维里迪安星弧（Pi）|
| 技术架构 / 开发计划 / 部署配置 | 两家高度一致，取交集 |